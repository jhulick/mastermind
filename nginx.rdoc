= Nginx

Install:

  sudo apt-get install nginx

Start:

  sudo nginx

Stop:

  sudo killall nginx

Restart:

  sudo killall -HUP nginx

Test configuration:

  sudo nginx -t

Test specific configuration file:

  sudo nginx -t -c [path_to_file]


